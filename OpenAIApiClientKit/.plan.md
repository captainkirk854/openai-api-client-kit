# Code Things

## Keep track of which parameters can be used for which models and which can't
e.g. 5.1 doesn't like -2.0 for Temparature or TopP cannot be used for certain models.
Build a ruleset into the builder or ChatCompletionRequest to auto-remove incompatible parameters.

## Consider some of architectural improvements:
e.g.

Adding a new model to the registry programmatically, with particular focus on the config
to pass to/use with the builder:

```
ModelRegistry.Register(new ModelDefinition
{
    Key = "gpt-4o-pro",
    Capabilities = { "chat", "vision" },
    Provider = OpenAIProvider.Instance,
    DefaultConfig = new ModelConfig
    {
        Temperature = 0.7f,
        MaxTokens = 4096
    }
});

```

## Change `ExplicitModel` to `ModelOverride` or `CustomModel` or something that implies it's a one-off override rather than a new model to use in the registry.`
e.g.
```
var request = new ChatRequest
{
    Messages = new []
    {
        new ChatMessage("user", "Write a haiku about recursion.")
    },
    ModelOverride = "gpt-4o-mini"
};

var response = await Orchestrator.ExecuteAsync(request);

```

## A Wrapper Class for the OpenAI API client 
Handles all interactions with the API, including sending requests, handling responses, and managing errors. This can help to abstract away the details of the API  provide a cleaner interface for the rest of the codebase.


# Model Costs

Find a way (web scraping) of getting the costs to feed into the OpenAIModels registry.

e.g.

```
[OpenAIModel.GPT5_2] = new ModelDescriptor
{
    Generation = OpenAIModelGeneration.GPT5,
    Domain = ModelDomain.Chat,
    Capabilities = new HashSet<ModelCapability>
    {
        ModelCapability.Text,
        ModelCapability.Chat,
        ModelCapability.Reasoning,
        ModelCapability.HighPerformance,
    },
    Pricing = new ModelPricing(
    inputTokenCost: 0.00000000m,
    outputTokenCost: 0.00000000m),
},
```

Do we get this via a webscraping tool or just do it by hand?

# Logging and Telemetry
Some form of logging and telemetry to track which models are being used, how often, and with what parameters. This can help with debugging, performance monitoring, 
and understanding user behaviour. Observability is key for a library like this, especially as it grows in complexity.

# Create a High-level facade class that allows easy access to the code.

# Housekeeping Things
## Ensure that all methods/class definitions use <cref="classname"/>
## Documentation must include available API details and usage examples
## Create some PowerShell scripts for simple OpenAI interactions.

# Create a Nuget Package

# Code Adjustments
For the current code that meets this, try and change:

1. For some of the code to use the EnsembleExecutor instead of the standard Orchestrator. 
   This will allow us to test out the multi‑model consolidation approaches.
1. Feed the Advanced Consolidater based not on specific models but on the capabilities of the models. 
1. For example, if the user prompt is asking for a creative story, we might want to feed it to all models with the "creative" capability and then consolidate their responses.
